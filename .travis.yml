language: python
python:
    - "3.6"

services:
    - docker

install:
    - pip install flake8
    - docker build . -t baselines-test

script:
    - docker run -it --rm --name agnes-test -v "$PWD":. python:3 python __init__.py